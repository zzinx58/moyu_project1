generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DEV_DATABASE_URL")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tournament_apply_t {
  id            Int     @id @default(autoincrement())
  t_id          Int
  p_id          Int
  p_name        String  @db.VarChar(255)
  user_id       Int
  name          String  @db.VarChar(255)
  identity_card String? @db.VarChar(255)
  tel           String? @db.VarChar(255)
  gender        String? @db.VarChar(255)
  region_id     Int?
  region        String? @db.VarChar(255)
  wca_id        String? @db.VarChar(255)
  apply_time    BigInt?
  verify        Int?    @db.TinyInt
  verify_time   BigInt?
  t_number      Int?

  @@index([identity_card], map: "identity_card")
  @@index([p_id], map: "p_id")
  @@index([t_id], map: "t_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tournament_result_t {
  id            BigInt  @id @default(autoincrement())
  t_id          Int
  p_id          Int
  p_name        String  @db.VarChar(255)
  user_id       Int
  name          String  @db.VarChar(255)
  phase         Int     @db.TinyInt
  r1_duration   Int
  r1_scramble   String? @db.VarChar(1024)
  r2_duration   Int
  r2_scramble   String? @db.VarChar(1024)
  r3_duration   Int
  r3_scramble   String? @db.VarChar(1024)
  r4_duration   Int
  r4_scramble   String? @db.VarChar(1024)
  r5_duration   Int
  r5_scramble   String? @db.VarChar(1024)
  best_duration Int
  avg           Int
  is_rise       Int?    @default(0) @db.TinyInt
  record_id     Int?    @default(0)
  cube_id       String? @db.VarChar(255)
  create_time   BigInt? @default(0)
  del_time      BigInt? @default(0)
  result        Int?    @default(0)
  is_del        Int?    @db.TinyInt
  t_number      Int?
  round_format  Int?    @db.TinyInt

  @@index([p_id], map: "p_id")
  @@index([phase], map: "phase")
  @@index([t_id], map: "t_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tournament_room_t {
  id         BigInt @id
  t_id       Int
  p_id       Int
  phase      Int    @db.TinyInt
  room_quota Int    @db.MediumInt
  room_limit Int    @db.MediumInt
  is_start   Int    @db.TinyInt
  time_1     BigInt
  time_2     BigInt
  rule_type  Int    @db.TinyInt
  room_list  String @db.Text
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tournament_schedule_t {
  id              BigInt @id
  t_id            Int
  p_id            Int
  p_name          String @db.VarChar(255)
  seq             Int    @db.TinyInt
  seq_time        String @db.VarChar(255)
  seq_name        String @db.VarChar(255)
  seq_icon        String @db.VarChar(255)
  phase           Int    @db.TinyInt
  rule_type       Int    @db.TinyInt
  pass_line       Int
  reduction_limit Int

  @@index([p_id], map: "p_id")
  @@index([t_id], map: "t_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tournament_t {
  id                      BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  type                    String?  @db.VarChar(255)
  on_off_line             Int      @db.TinyInt
  name                    String?  @db.VarChar(255)
  sub_name                String?  @db.VarChar(255)
  apply_time_1            BigInt?
  apply_time_2            BigInt?
  r1_time_1               BigInt?
  r1_time_2               BigInt?
  r2_time_1               BigInt?
  r2_time_2               BigInt?
  r3_time_1               BigInt?
  r3_time_2               BigInt?
  close_time              BigInt?
  organizer               String?  @db.VarChar(255)
  location                String?  @db.VarChar(255)
  apply_quota             Int?     @db.MediumInt
  r1_quota                Int?     @db.MediumInt
  r1_rule                 String?  @db.VarChar(1024)
  r1_rule_type            Int?     @db.TinyInt
  r1_pass_line            Int?     @db.MediumInt
  r2_quota                Int?     @db.MediumInt
  r2_rule                 String?  @db.VarChar(1024)
  r2_rule_type            Int?     @db.TinyInt
  r2_pass_line            Int?     @db.MediumInt
  r3_rule                 String?  @db.VarChar(1024)
  r3_rule_type            Int?     @db.TinyInt
  room_quota              Int?     @db.MediumInt
  room_limit              Int?     @db.MediumInt
  rule                    String?  @db.VarChar(1024)
  organizer_web_url       String?  @db.VarChar(255)
  groups                  String?  @db.VarChar(255)
  apply_condition         String?  @db.VarChar(255)
  apply_condition_remarks String?  @db.VarChar(255)
  published               Boolean? @default(true)
  apply_time_range        Json?
  time_range              Json?
  projects_detail         Json?
  display_order           Int?
  display_on_homepage     Boolean?
  banner                  Json?
  type_id                 Int?
  apply_web_url           String?  @db.VarChar(255)
  apply_image_url         String?  @db.VarChar(255)
  audit_status            Int?     @default(2)
  award_time_range        Json?
  signin_time_range       Json?
  break_time              Json?
  lottery_time            Json?
  projects                String?  @db.VarChar(255)
  status_id               Int?     @db.TinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model act_affirm_t {
  act_index   Int
  act_version Int
  value       Int
  reward_user Int

  @@id([act_index, act_version])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model act_affirm_user_t {
  user_id     Int
  act_index   Int
  act_version Int
  value       Int
  value1      Int
  value2      Int
  value3      Int

  @@id([user_id, act_index, act_version])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model admin_assets_t {
  id           Int    @id
  user_id      Int
  assets       Json
  content      String @db.VarChar(1023)
  create_time  Int    @db.UnsignedInt
  receive_time Int    @db.UnsignedInt
  receive_num  Int

  @@index([create_time], map: "create_time")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ai_teach_stat_t {
  user_id      Int  @id
  affirm_time  Int? @default(0)
  college_time Int? @default(0)
  ai_time      Int? @default(0)
  ai_pass_time Int? @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model app_eval_log_t {
  id          Int @id
  user_id     Int
  trigger_id  Int
  is_close    Int @db.TinyInt
  is_eval     Int @db.TinyInt
  is_feedback Int @db.TinyInt
  time        Int @db.UnsignedInt

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ban_log_t {
  id           Int    @id @default(autoincrement())
  type         Int    @db.TinyInt
  user_id      Int
  machine_code String @db.VarChar(255)
  state        Int    @db.TinyInt
  ban_time     Int    @db.UnsignedInt
  manual_time  Int    @default(0)
  admin_user   String @db.VarChar(255)
  op_type      Int    @db.TinyInt
  reason       String @db.VarChar(8000)
  action_time  Int    @db.UnsignedInt

  @@index([machine_code], map: "machine_code")
  @@index([op_type], map: "op_type")
  @@index([type], map: "type")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ban_t {
  id           Int    @id
  type         Int    @db.TinyInt
  user_id      Int
  machine_code String @db.VarChar(255)
  state        Int    @db.TinyInt
  ban_time     Int    @default(0) @db.UnsignedInt
  action_time  Int    @db.UnsignedInt
  reason       String @db.VarChar(8000)
  admin_user   String @db.VarChar(255)

  @@index([machine_code], map: "machine_code")
  @@index([type], map: "type")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cd_key_order_t {
  code        String @id @db.VarChar(255)
  type        Int
  user_id     Int?
  create_time Int    @db.UnsignedInt

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cd_key_t {
  id      Int    @id @default(autoincrement())
  type    Int    @db.TinyInt
  code    String @db.VarChar(20)
  user_id Int?

  @@index([code], map: "code")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model certificate_support_t {
  id          Int @id
  support_id  Int
  apply_id    Int
  type        Int @db.TinyInt
  create_time Int @db.UnsignedInt
  result      Int @db.TinyInt
  result_time Int @db.UnsignedInt
  notice_time Int @db.UnsignedInt

  @@index([apply_id], map: "apply_id")
  @@index([support_id], map: "support_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model certificate_t {
  id             Int    @id
  user_id        Int
  type           Int    @db.TinyInt
  content        String @db.VarChar(255)
  create_time    Int    @db.UnsignedInt
  result         Int    @db.TinyInt
  result_time    Int    @db.UnsignedInt
  result_content String @db.Text
  delete_time    Int    @db.UnsignedInt
  id_no          String @db.VarChar(255)
  id_name        String @db.VarChar(255)
  upload         Json
  is_show        Int    @db.TinyInt

  @@index([type], map: "type")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model chat_t {
  user_id  Int
  oppo_id  Int
  count    Int    @default(0) @db.UnsignedInt
  chat_msg String @db.MediumText

  @@id([user_id, oppo_id])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model clock_log_t {
  id           Int      @id @default(autoincrement())
  user_id      Int      @db.UnsignedInt
  clock_type   Int      @db.UnsignedTinyInt
  start_mtime  BigInt
  log_time     DateTime @db.DateTime(0)
  result       Int      @db.TinyInt
  duration     Int
  turn         Int      @default(0) @db.SmallInt
  smooth_index Int      @default(0) @db.SmallInt
  tps          Float    @default(0.00) @db.Float
  ao5          Int      @default(0)
  ao12         Int
  ao100        Int
  duration_c   Int
  duration_f   Int
  duration_o   Int
  duration_p   Int

  @@unique([user_id, clock_type, start_mtime], map: "user_clock")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model clock_mark_t {
  id           Int    @id @default(autoincrement())
  user_id      Int    @db.UnsignedInt
  clock_type   Int    @db.TinyInt
  start_mtime  BigInt
  result       Int    @db.TinyInt
  duration     Int
  turn         Int    @default(0) @db.SmallInt
  rotate       Int    @default(0) @db.SmallInt
  smooth_index Int    @default(0) @db.SmallInt
  cube_model   String @db.VarChar(255)
  scramble     String @default("") @db.VarChar(2048)
  review_id    Int
  mark_time    Int    @default(0) @db.UnsignedInt

  @@unique([user_id, start_mtime], map: "user_clock")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model clock_record_t {
  id           Int     @id @default(autoincrement())
  user_id      Int     @db.UnsignedInt
  clock_type   Int     @db.UnsignedTinyInt
  group_id     Int     @db.UnsignedTinyInt
  start_mtime  BigInt
  result       Int     @db.TinyInt
  duration     Int
  turn         Int     @default(0) @db.SmallInt
  rotate       Int     @default(0) @db.SmallInt
  smooth_index Int     @default(0) @db.SmallInt
  cube_model   String  @db.VarChar(255)
  scramble     String  @default("") @db.VarChar(2048)
  review_id    Int
  deleted      Boolean @default(false)

  @@unique([user_id, clock_type, group_id, start_mtime], map: "user_clock")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model clock_stat_agg_t {
  id              Int   @id @default(autoincrement())
  user_id         Int   @db.UnsignedInt
  clock_type      Int   @db.UnsignedTinyInt
  group_id        Int   @db.UnsignedTinyInt
  aggregate_type  Int   @db.TinyInt
  aggregate_index Int
  duration        Int
  turn            Int   @default(0) @db.SmallInt
  smooth_index    Int   @default(0) @db.SmallInt
  tps             Float @default(0.00) @db.Float
  ao5             Int   @default(0)
  ao12            Int
  ao100           Int

  @@unique([user_id, clock_type, group_id, aggregate_type, aggregate_index], map: "user_clock")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model clock_stat_best_t {
  id           Int   @id @default(autoincrement())
  user_id      Int   @db.UnsignedInt
  clock_type   Int   @db.UnsignedTinyInt
  group_id     Int   @db.UnsignedTinyInt
  best_type    Int   @db.TinyInt
  duration     Int
  turn         Int   @default(0) @db.SmallInt
  smooth_index Int   @default(0) @db.SmallInt
  tps          Float @default(0.00) @db.Float
  ao5          Int   @default(0)
  ao12         Int
  ao100        Int

  @@unique([user_id, clock_type, group_id, best_type], map: "user_clock")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model clock_stat_t {
  id           Int    @id @default(autoincrement())
  user_id      Int    @db.UnsignedInt
  clock_type   Int    @db.UnsignedTinyInt
  group_id     Int    @db.UnsignedTinyInt
  start_mtime  BigInt
  days         Int    @db.SmallInt
  weeks        Int    @db.SmallInt
  result       Int    @db.TinyInt
  duration     Int
  turn         Int    @default(0) @db.SmallInt
  smooth_index Int    @default(0) @db.SmallInt
  tps          Float  @default(0.00) @db.Float
  ao5          Int    @default(0)
  ao12         Int
  ao100        Int

  @@unique([user_id, clock_type, group_id, start_mtime], map: "user_clock")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model comp_1v1_record_t {
  id                  Int      @id @default(autoincrement())
  winner              Int
  result_type         Int      @db.TinyInt
  user_id_1           Int
  user_id_2           Int
  start_time          Int      @db.UnsignedInt
  duration_list_1     String   @db.VarChar(1023)
  duration_list_2     String   @db.VarChar(1023)
  turn_list_1         String   @db.VarChar(1023)
  turn_list_2         String   @db.VarChar(1023)
  rotate_list_1       String   @db.VarChar(1023)
  rotate_list_2       String   @db.VarChar(1023)
  smooth_index_list_1 String   @db.VarChar(1023)
  smooth_index_list_2 String   @db.VarChar(1023)
  review_id_list_1    String   @default("[]") @db.VarChar(1023)
  review_id_list_2    String   @default("[]") @db.VarChar(1023)
  gmt_create          DateTime @default(now()) @db.Timestamp(0)

  @@index([user_id_1], map: "user_id_1")
  @@index([user_id_2], map: "user_id_2")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model comp_multi_record_t {
  user_id        Int
  start_time     Int    @db.UnsignedInt
  comp_id        Int
  round_defined  Int    @db.TinyInt
  weed_out_time  Int    @db.UnsignedInt
  record_id      Int
  result         Int    @db.TinyInt
  duration_list  String @db.VarChar(1023)
  exception_list String @default("[]") @db.VarChar(1023)
  review_id_list String @db.VarChar(1023)
  score_list     String @db.VarChar(1023)
  team           Int

  @@id([user_id, start_time])
  @@index([comp_id], map: "comp_id")
  @@index([record_id], map: "record_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cube_clone_record_t {
  user_id         Int
  start_time      Int    @db.UnsignedInt
  reduction_mtime BigInt
  result          Int    @db.TinyInt
  duration        Int
  turn            Int    @db.SmallInt
  rotate          Int    @db.SmallInt
  smooth_index    Int    @db.SmallInt
  scramble        String @db.VarChar(255)
  cube_model      String @db.VarChar(255)
  reduction       String @db.VarChar(1024)
  review_id       Int

  @@id([user_id, start_time])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model discuss_praise_t {
  user_id    Int
  discuss_id Int

  @@id([user_id, discuss_id])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model discuss_t {
  id            Int    @id
  main_id       Int
  reply_id      Int
  sys_id        Int    @db.SmallInt
  subject_id    Int
  user_id       Int
  reply_user_id Int
  content       String @db.MediumText
  praise        Int
  trample       Int
  create_time   Int    @db.UnsignedInt
  is_delete     Int    @db.TinyInt

  @@index([create_time], map: "create_time")
  @@index([main_id], map: "main_id")
  @@index([reply_id], map: "reply_id")
  @@index([subject_id], map: "subject_id")
  @@index([sys_id], map: "sys_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model equipment_affirm_t {
  user_id     Int
  address     String @db.VarChar(255)
  type        String @db.VarChar(255)
  model       String @db.VarChar(255)
  name        String @db.VarChar(255)
  affirm_time Int    @db.UnsignedInt
  delete_time Int    @db.UnsignedInt

  @@id([user_id, address])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model equipment_t {
  address         String    @id @db.VarChar(255)
  type            String    @db.VarChar(255)
  model           String    @db.VarChar(255)
  version         String    @default("") @db.VarChar(255)
  name            String    @default("") @db.VarChar(255)
  first_conn_time DateTime? @db.DateTime(0)
  first_conn_user Int
  last_conn_time  DateTime? @db.DateTime(0)
  last_conn_user  Int
  bind            Int       @default(0)
  use_count       Int       @default(0)
  count1          Int       @default(0)
  count2          Int       @default(0)
  count3          Int       @default(0)
  count4          Int       @default(0)
  count5          Int       @default(0)
  count6          Int       @default(0)
  usage_time      Int       @default(0)
  reset_count     Int       @default(0)
  gmt_modified    DateTime  @default(now()) @db.Timestamp(0)
  gmt_create      DateTime  @default(now()) @db.Timestamp(0)

  @@index([first_conn_user], map: "first_conn_user")
  @@index([last_conn_user], map: "last_conn_user")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model exchange_order_t {
  id             BigInt   @id
  user_id        Int
  shop_id        Int
  item_type      Int      @db.SmallInt
  item_id        Int
  item_name      String   @db.VarChar(255)
  item_num       Int
  cost_item_id   Int
  cost_item_name String   @db.VarChar(255)
  cost_item_num  Int
  cost_data      Json
  time           Int
  consignee      String   @db.VarChar(255)
  phone          String   @db.VarChar(255)
  postal_code    String   @db.VarChar(255)
  country        String   @db.VarChar(255)
  province       String   @db.VarChar(255)
  city           String   @db.VarChar(255)
  tax_id         String   @db.VarChar(255)
  address        String   @db.VarChar(1023)
  order_state    Int      @db.TinyInt
  express        String   @db.VarChar(1023)
  remark         String   @db.VarChar(1023)
  data           Json
  gmt_modified   DateTime @default(now()) @db.Timestamp(0)
  gmt_create     DateTime @default(now()) @db.Timestamp(0)

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model friend_presented_t {
  id          Int @id
  sender      Int
  accepter    Int
  item_id     Int
  item_num    Int
  send_time   Int @db.UnsignedInt
  accept_time Int @db.UnsignedInt
  activity_id Int

  @@index([accepter], map: "accepter")
  @@index([sender], map: "sender")
}

model global_data_kv {
  id           Bytes    @id @db.VarBinary(255)
  data         String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model guild_apply_t {
  id       Int    @id @default(autoincrement())
  guild_id Int
  user_id  Int
  type     Int    @db.TinyInt
  reason   String @db.VarChar(255)
  time     BigInt

  @@index([guild_id], map: "guild_id")
  @@index([type], map: "type")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model guild_member_t {
  user_id  Int    @id
  guild_id Int
  duty     Int    @db.TinyInt
  time     BigInt
  power    Int    @default(0)

  @@index([guild_id], map: "guild_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model guild_t {
  id             Int    @id
  status         Int    @db.TinyInt
  name           String @db.VarChar(255)
  level          Int
  declaration    String @db.VarChar(1024)
  notice         String @db.VarChar(1024)
  duration_need  Int
  qualifier_need Int
  max_members    Int
  region_id      Int
  leader         Int
  members        String @db.MediumText
  sign           String @db.VarChar(1024)
  chats          String @db.Text
  grade          BigInt
  count          Int
  power          Int
  time           BigInt

  @@index([status], map: "status")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model helper_t {
  id         Int    @id @default(autoincrement())
  title      String @default("") @db.VarChar(255)
  content    String @db.MediumText
  sub_titles String @default("") @db.VarChar(255)
  jumps      String @default("") @db.VarChar(255)
  praise     Int    @default(0)
  trample    Int    @default(0)
  num        Int    @default(0)
  deleted    Int    @default(0) @db.TinyInt

  @@index([num], map: "num")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model index_t {
  k     String @id @db.VarChar(255)
  v     Int?   @db.UnsignedInt
  v_old Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model invitation_code_t {
  id          Int    @id
  code        String @db.VarChar(255)
  type        Int    @db.TinyInt
  creator_id  Int
  create_time Int    @db.UnsignedInt
  invitee_ids String @db.MediumText
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model issue_t {
  id         Int    @id @default(autoincrement())
  user_id    Int
  issue_time Int    @db.UnsignedInt
  content    String @db.MediumText
  pictures   String @db.VarChar(10230)
  reply      String @db.MediumText
  reply_time Int    @db.UnsignedInt

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model log_activity_affirm {
  id           Int      @id @default(autoincrement())
  user_id      Int
  log_time     DateTime @db.DateTime(0)
  log_date     DateTime @db.Date
  log_hour     Int      @default(0) @db.TinyInt
  country      String   @default("") @db.VarChar(255)
  channel_id   Int      @default(0)
  data         Json?
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([channel_id], map: "channel_id")
  @@index([country], map: "country")
  @@index([log_date], map: "log_date")
  @@index([log_hour], map: "log_hour")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model log_activity_rank_city {
  id           Int      @id @default(autoincrement())
  user_id      Int
  log_time     DateTime @db.DateTime(0)
  log_date     DateTime @db.Date
  log_hour     Int      @default(0) @db.TinyInt
  country      String   @default("") @db.VarChar(255)
  channel_id   Int      @default(0)
  data         Json?
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([channel_id], map: "channel_id")
  @@index([country], map: "country")
  @@index([log_date], map: "log_date")
  @@index([log_hour], map: "log_hour")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model log_comp_1v1 {
  id           Int      @id @default(autoincrement())
  user_id      Int
  log_time     DateTime @db.DateTime(0)
  log_date     DateTime @db.Date
  log_hour     Int      @default(0) @db.TinyInt
  country      String   @default("") @db.VarChar(255)
  channel_id   Int      @default(0)
  data         Json?
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([channel_id], map: "channel_id")
  @@index([country], map: "country")
  @@index([log_date], map: "log_date")
  @@index([log_hour], map: "log_hour")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model log_comp_multi {
  id           Int      @id @default(autoincrement())
  user_id      Int
  log_time     DateTime @db.DateTime(0)
  log_date     DateTime @db.Date
  log_hour     Int      @default(0) @db.TinyInt
  country      String   @default("") @db.VarChar(255)
  channel_id   Int      @default(0)
  data         Json?
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([channel_id], map: "channel_id")
  @@index([country], map: "country")
  @@index([log_date], map: "log_date")
  @@index([log_hour], map: "log_hour")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model log_cube_clone {
  id           Int      @id @default(autoincrement())
  user_id      Int
  log_time     DateTime @db.DateTime(0)
  log_date     DateTime @db.Date
  log_hour     Int      @default(0) @db.TinyInt
  country      String   @default("") @db.VarChar(255)
  channel_id   Int      @default(0)
  data         Json?
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([channel_id], map: "channel_id")
  @@index([country], map: "country")
  @@index([log_date], map: "log_date")
  @@index([log_hour], map: "log_hour")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model log_double_11_badge_collect {
  id           Int      @id @default(autoincrement())
  user_id      Int
  log_time     DateTime @db.DateTime(0)
  log_date     DateTime @db.Date
  log_hour     Int      @default(0) @db.TinyInt
  country      String   @default("") @db.VarChar(255)
  channel_id   Int      @default(0)
  data         Json?
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([channel_id], map: "channel_id")
  @@index([country], map: "country")
  @@index([log_date], map: "log_date")
  @@index([log_hour], map: "log_hour")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model log_double_11_h5_click {
  id           Int      @id @default(autoincrement())
  user_id      Int
  log_time     DateTime @db.DateTime(0)
  log_date     DateTime @db.Date
  log_hour     Int      @default(0) @db.TinyInt
  country      String   @default("") @db.VarChar(255)
  channel_id   Int      @default(0)
  data         Json?
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([channel_id], map: "channel_id")
  @@index([country], map: "country")
  @@index([log_date], map: "log_date")
  @@index([log_hour], map: "log_hour")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model log_double_11_invitation_code {
  id           Int      @id @default(autoincrement())
  user_id      Int
  log_time     DateTime @db.DateTime(0)
  log_date     DateTime @db.Date
  log_hour     Int      @default(0) @db.TinyInt
  country      String   @default("") @db.VarChar(255)
  channel_id   Int      @default(0)
  data         Json?
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([channel_id], map: "channel_id")
  @@index([country], map: "country")
  @@index([log_date], map: "log_date")
  @@index([log_hour], map: "log_hour")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model log_equipment_disaffirm {
  id           Int      @id @default(autoincrement())
  user_id      Int
  log_time     DateTime @db.DateTime(0)
  log_date     DateTime @db.Date
  log_hour     Int      @default(0) @db.TinyInt
  country      String   @default("") @db.VarChar(255)
  channel_id   Int      @default(0)
  data         Json?
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([channel_id], map: "channel_id")
  @@index([country], map: "country")
  @@index([log_date], map: "log_date")
  @@index([log_hour], map: "log_hour")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model log_helper_words_search {
  id           Int      @id @default(autoincrement())
  user_id      Int
  log_time     DateTime @db.DateTime(0)
  log_date     DateTime @db.Date
  log_hour     Int      @default(0) @db.TinyInt
  country      String   @default("") @db.VarChar(255)
  channel_id   Int      @default(0)
  data         Json?
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([channel_id], map: "channel_id")
  @@index([country], map: "country")
  @@index([log_date], map: "log_date")
  @@index([log_hour], map: "log_hour")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model log_lotto_turn {
  id           Int      @id @default(autoincrement())
  user_id      Int
  log_time     DateTime @db.DateTime(0)
  log_date     DateTime @db.Date
  log_hour     Int      @default(0) @db.TinyInt
  country      String   @default("") @db.VarChar(255)
  channel_id   Int      @default(0)
  data         Json?
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([channel_id], map: "channel_id")
  @@index([country], map: "country")
  @@index([log_date], map: "log_date")
  @@index([log_hour], map: "log_hour")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model log_play_time {
  id           Int      @id @default(autoincrement())
  user_id      Int
  log_time     DateTime @db.DateTime(0)
  log_date     DateTime @db.Date
  log_hour     Int      @default(0) @db.TinyInt
  country      String   @default("") @db.VarChar(255)
  channel_id   Int      @default(0)
  data         Json?
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([channel_id], map: "channel_id")
  @@index([country], map: "country")
  @@index([log_date], map: "log_date")
  @@index([log_hour], map: "log_hour")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model log_start_device {
  id           Int      @id @default(autoincrement())
  user_id      Int
  log_time     DateTime @db.DateTime(0)
  log_date     DateTime @db.Date
  log_hour     Int      @default(0) @db.TinyInt
  country      String   @default("") @db.VarChar(255)
  channel_id   Int      @default(0)
  data         Json?
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([channel_id], map: "channel_id")
  @@index([country], map: "country")
  @@index([log_date], map: "log_date")
  @@index([log_hour], map: "log_hour")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model log_user_equipment_conn {
  id           Int      @id @default(autoincrement())
  user_id      Int
  log_time     DateTime @db.DateTime(0)
  log_date     DateTime @db.Date
  log_hour     Int      @default(0) @db.TinyInt
  country      String   @default("") @db.VarChar(255)
  channel_id   Int      @default(0)
  data         Json?
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([channel_id], map: "channel_id")
  @@index([country], map: "country")
  @@index([log_date], map: "log_date")
  @@index([log_hour], map: "log_hour")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model log_user_item {
  id           Int      @id @default(autoincrement())
  user_id      Int
  log_time     DateTime @db.DateTime(0)
  log_date     DateTime @db.Date
  log_hour     Int      @default(0) @db.TinyInt
  country      String   @default("") @db.VarChar(255)
  channel_id   Int      @default(0)
  data         Json?
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([channel_id], map: "channel_id")
  @@index([country], map: "country")
  @@index([log_date], map: "log_date")
  @@index([log_hour], map: "log_hour")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model log_user_login_logout {
  id             Int       @id @default(autoincrement())
  user_id        Int
  login_time     DateTime  @db.DateTime(0)
  login_date     DateTime  @db.Date
  login_hour     Int       @default(0) @db.TinyInt
  login_type     Int       @default(0) @db.TinyInt
  login_visitor  Boolean
  on_time        DateTime? @db.DateTime(0)
  on_date        DateTime? @db.Date
  on_hour        Int?      @db.TinyInt
  logout_time    DateTime? @db.DateTime(0)
  logout_date    DateTime? @db.Date
  logout_hour    Int?      @db.TinyInt
  logout_type    Int?      @db.TinyInt
  logout_visitor Boolean?
  country        String    @default("") @db.VarChar(255)
  data           Json?
  gmt_modified   DateTime  @default(now()) @db.Timestamp(0)
  gmt_create     DateTime  @default(now()) @db.Timestamp(0)

  @@index([country], map: "country")
  @@index([login_date], map: "login_date")
  @@index([login_type], map: "login_type")
  @@index([login_visitor], map: "login_visitor")
  @@index([logout_date], map: "logout_date")
  @@index([logout_type], map: "logout_type")
  @@index([logout_visitor], map: "logout_visitor")
  @@index([on_date], map: "on_date")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model log_user_money {
  id           Int      @id @default(autoincrement())
  user_id      Int
  log_time     DateTime @db.DateTime(0)
  log_date     DateTime @db.Date
  log_hour     Int      @default(0) @db.TinyInt
  country      String   @default("") @db.VarChar(255)
  channel_id   Int      @default(0)
  data         Json?
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([channel_id], map: "channel_id")
  @@index([country], map: "country")
  @@index([log_date], map: "log_date")
  @@index([log_hour], map: "log_hour")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model log_user_online {
  id           Int      @id @default(autoincrement())
  user_id      Int
  log_time     DateTime @db.DateTime(0)
  log_date     DateTime @db.Date
  log_hour     Int      @default(0) @db.TinyInt
  country      String   @default("") @db.VarChar(255)
  channel_id   Int      @default(0)
  data         Json?
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([channel_id], map: "channel_id")
  @@index([country], map: "country")
  @@index([log_date], map: "log_date")
  @@index([log_hour], map: "log_hour")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model log_user_register {
  id           Int      @id @default(autoincrement())
  user_id      Int
  log_time     DateTime @db.DateTime(0)
  log_date     DateTime @db.Date
  log_hour     Int      @default(0) @db.TinyInt
  country      String   @default("") @db.VarChar(255)
  channel_id   Int      @default(0)
  data         Json?
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([channel_id], map: "channel_id")
  @@index([country], map: "country")
  @@index([log_date], map: "log_date")
  @@index([log_hour], map: "log_hour")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model log_user_task {
  id           Int      @id @default(autoincrement())
  user_id      Int
  log_time     DateTime @db.DateTime(0)
  log_date     DateTime @db.Date
  log_hour     Int      @default(0) @db.TinyInt
  country      String   @default("") @db.VarChar(255)
  channel_id   Int      @default(0)
  data         Json?
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([channel_id], map: "channel_id")
  @@index([country], map: "country")
  @@index([log_date], map: "log_date")
  @@index([log_hour], map: "log_hour")
  @@index([user_id], map: "user_id")
}

model off_event_kv {
  id           Int      @id
  data         String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model old_acc {
  id       Int    @id @default(autoincrement())
  acc_info String @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model play_score_t {
  id          Int      @id @default(autoincrement())
  user_id     Int
  sys_name    String   @db.VarChar(255)
  sub_name    String   @db.VarChar(255)
  nth         Boolean
  comment_id  Int
  score       Int      @db.SmallInt
  comment     String   @db.MediumText
  play_time   Int
  os          String   @db.VarChar(255)
  equip_model String   @db.VarChar(255)
  create_time DateTime @default(now()) @db.DateTime(0)

  @@index([comment_id], map: "comment_id")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model play_time_t {
  user_id   Int
  sys_name  String   @db.VarChar(255)
  sub_name  String   @db.VarChar(255)
  play_date DateTime @db.Date
  duration  Int

  @@id([user_id, sys_name, sub_name, play_date])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model questionnaire_t {
  id       Int @id @default(autoincrement())
  user_id  Int
  title_id Int
  choose_a Int @default(0) @db.TinyInt
  choose_b Int @default(0) @db.TinyInt
  choose_c Int @default(0) @db.TinyInt
  choose_d Int @default(0) @db.TinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model questionnaire_text_t {
  id       Int     @id @default(autoincrement())
  user_id  Int
  title_id Int
  answer   String? @db.MediumText
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_cube_clone_0 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_cube_clone_stat {
  user_id   Int    @id
  rank_best Int
  rank_list String @db.MediumText
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_qualifier_0 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_qualifier_1 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_qualifier_2 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_qualifier_3 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_qualifier_4 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_qualifier_5 {
  user_id      Int       @id
  rank_value   Int
  other_info   String    @db.MediumText
  gmt_modified DateTime? @default(now()) @db.Timestamp(0)
  gmt_create   DateTime? @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_qualifier_stat {
  user_id   Int    @id
  rank_best Int
  rank_list String @db.MediumText
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_standard_clock_0 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_standard_clock_stat {
  user_id   Int    @id
  rank_best Int
  rank_list String @db.MediumText
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_10 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_11 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_12 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_121 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_122 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_123 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_124 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_125 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_126 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_127 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_13 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_14 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_15 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_16 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_17 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_18 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_19 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_20 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_21 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_22 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_23 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_24 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_25 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_26 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_27 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_28 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_29 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_30 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_31 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_32 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_33 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_34 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_35 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_36 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_37 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_38 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_39 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_40 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_41 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_42 {
  user_id      Int       @id
  rank_value   Int
  other_info   String    @db.MediumText
  gmt_modified DateTime? @default(now()) @db.Timestamp(0)
  gmt_create   DateTime? @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_43 {
  user_id      Int       @id
  rank_value   Int
  other_info   String    @db.MediumText
  gmt_modified DateTime? @default(now()) @db.Timestamp(0)
  gmt_create   DateTime? @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_45 {
  user_id      Int       @id
  rank_value   Int
  other_info   String    @db.MediumText
  gmt_modified DateTime? @default(now()) @db.Timestamp(0)
  gmt_create   DateTime? @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_46 {
  user_id      Int       @id
  rank_value   Int
  other_info   String    @db.MediumText
  gmt_modified DateTime? @default(now()) @db.Timestamp(0)
  gmt_create   DateTime? @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_47 {
  user_id      Int       @id
  rank_value   Int
  other_info   String    @db.MediumText
  gmt_modified DateTime? @default(now()) @db.Timestamp(0)
  gmt_create   DateTime? @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_48 {
  user_id      Int       @id
  rank_value   Int
  other_info   String    @db.MediumText
  gmt_modified DateTime? @default(now()) @db.Timestamp(0)
  gmt_create   DateTime? @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_49 {
  user_id      Int       @id
  rank_value   Int
  other_info   String    @db.MediumText
  gmt_modified DateTime? @default(now()) @db.Timestamp(0)
  gmt_create   DateTime? @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_50 {
  user_id      Int       @id
  rank_value   Int
  other_info   String    @db.MediumText
  gmt_modified DateTime? @default(now()) @db.Timestamp(0)
  gmt_create   DateTime? @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_51 {
  user_id      Int       @id
  rank_value   Int
  other_info   String    @db.MediumText
  gmt_modified DateTime? @default(now()) @db.Timestamp(0)
  gmt_create   DateTime? @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_52 {
  user_id      Int       @id
  rank_value   Int
  other_info   String    @db.MediumText
  gmt_modified DateTime? @default(now()) @db.Timestamp(0)
  gmt_create   DateTime? @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_53 {
  user_id      Int       @id
  rank_value   Int
  other_info   String    @db.MediumText
  gmt_modified DateTime? @default(now()) @db.Timestamp(0)
  gmt_create   DateTime? @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_7 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_8 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_9 {
  user_id      Int      @id
  rank_value   Int
  other_info   String   @db.MediumText
  gmt_modified DateTime @default(now()) @db.Timestamp(0)
  gmt_create   DateTime @default(now()) @db.Timestamp(0)

  @@index([rank_value], map: "rank_value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rank_time_trial_stat {
  user_id   Int    @id
  rank_best Int
  rank_list String @db.MediumText
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model report_t {
  id             Int    @id
  user_id        Int
  behaviour      Int    @db.TinyInt
  content        String @db.MediumText
  review_id      Int
  reduction      String @default("") @db.VarChar(14336)
  commit_user_id Int
  time           Int
  result         Int    @db.TinyInt

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model review_note_t {
  review_id Int  @id
  user_id   Int
  hide      Int  @db.TinyInt
  note      Json
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model review_solution_t {
  review_id      Int
  type           Int
  order_step     Int
  user_id        Int
  total_duration Int
  duration       Int
  turn           Int   @db.SmallInt
  tps            Float @db.Float
  rotate         Int   @db.SmallInt
  smooth_index   Int   @db.SmallInt
  observe_time   Int
  adjust_time    Int
  execute_time   Int
  auf_time       Int
  start_time     Int   @db.UnsignedInt
  start_step     Int
  over_step      Int

  @@id([review_id, type, order_step])
  @@index([start_time], map: "start_time")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model review_t {
  review_id      Int    @id
  user_id        Int
  praise         Int
  type           Int    @db.SmallInt
  cube_model     String @db.VarChar(255)
  start_time     Int    @db.UnsignedInt
  exception_type Int    @db.TinyInt
  duration       Int
  turn           Int    @db.SmallInt
  rotate         Int    @db.SmallInt
  smooth_index   Int    @db.SmallInt
  scramble       String @db.VarChar(255)
  scramble_from  Int    @db.SmallInt
  scramble_to    Int    @db.SmallInt
  reduction      String @default("") @db.VarChar(14336)
  review         String @db.MediumText
  version        Int    @db.SmallInt
  solution_type  Int    @db.SmallInt
  down_color     Int    @db.TinyInt
  oll_index      Int    @default(0) @db.TinyInt
  pll_index      Int    @default(0) @db.TinyInt

  @@index([duration], map: "duration")
  @@index([start_time], map: "start_time")
  @@index([type], map: "type")
  @@index([user_id], map: "user_id")
}

model schema_migrations {
  version     BigInt    @id
  inserted_at DateTime? @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sys_inform_t {
  inform_id    Int    @id
  ad_inform_id Int
  ad_switch    Int    @db.TinyInt
  c_inform_id  Int
  start_time   Int    @db.UnsignedInt
  end_time     Int    @db.UnsignedInt
  title        String @db.VarChar(255)
  en_title     String @db.VarChar(255)
  param_l      String @db.MediumText
  en_param_l   String @db.MediumText
  attach_l     String @db.MediumText
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model terminal_t {
  user_id          Int
  machine_code     String @db.VarChar(255)
  terminal_type    Int    @db.TinyInt
  terminal_name    String @db.VarChar(255)
  terminal_version String @db.VarChar(255)
  terminal_vendor  String @db.VarChar(255)
  terminal_size    String @db.VarChar(255)
  terminal_cpu     String @db.VarChar(255)
  last_use_time    Int    @db.UnsignedInt

  @@id([user_id, machine_code])
  @@index([machine_code], map: "machine_code")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model time_trial_record_t {
  user_id         Int
  start_time      Int    @db.UnsignedInt
  reduction_mtime BigInt
  result          Int    @db.TinyInt
  exception_type  Int    @db.TinyInt
  duration        Int
  turn            Int    @db.SmallInt
  rotate          Int    @db.SmallInt
  smooth_index    Int    @db.SmallInt
  scramble        String @db.VarChar(255)
  cube_model      String @db.VarChar(255)
  reduction       String @db.VarChar(1024)
  review_id       Int

  @@id([user_id, start_time])
  @@index([exception_type], map: "exception_type")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model train_defined_share_formula_t {
  id            Int    @id
  share_id      Int
  formula_id    Int
  formula       String @db.VarChar(1023)
  formula_name  String @db.VarChar(1023)
  orientation   String @db.VarChar(1023)
  state_id      Int
  init_state_id String @db.VarChar(1023)
  intro         String @db.VarChar(1023)
  num           Int
  is_delete     Int    @db.TinyInt

  @@index([formula_id], map: "formula_id")
  @@index([share_id], map: "share_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model train_defined_share_repo_t {
  id           Int    @id
  repo_name    String @db.VarChar(255)
  user_id      Int
  target_ids   String @db.VarChar(10230)
  intro        String @db.VarChar(1023)
  share_time   Int    @db.UnsignedInt
  praise       Int
  praise_json  Json
  trample      Int
  trample_json Json
  formula_num  Int
  heat         Int
  discuss_num  Int
  label        Json
  is_delete    Int    @db.TinyInt

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model train_formula_t {
  id          Int     @id
  user_id     Int
  formula_id  Int
  formula     String  @db.VarChar(1023)
  train_count Int
  again_count Int
  total_mtime Int
  total_turn  Int
  grasp       Boolean
  score       Int     @db.SmallInt

  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model train_log_t {
  id         Int      @id @default(autoincrement())
  user_id    Int
  cube_type  Boolean
  train_type Int      @db.TinyInt
  formula_id Int
  log_type   Boolean
  train_time Int      @db.UnsignedInt
  cost_mtime Int
  turn       Int
  avg_diff   Int
  log_time   DateTime @db.DateTime(0)

  @@index([formula_id], map: "formula_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model train_record_t {
  id         Int    @id @default(autoincrement())
  user_id    Int
  train_type Int
  formula_id Int
  formula    String @default("") @db.VarChar(1023)
  train_time Int    @db.UnsignedInt
  cost_mtime Int
  turn       Int
  avg_diff   Int

  @@index([formula_id], map: "formula_id")
  @@index([train_time], map: "train_time")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model train_solution_share_t {
  id         Int    @id
  formula_id Int
  formula    String @db.VarChar(1023)
  user_id    Int
  share_time Int    @db.UnsignedInt
  num        Int
  is_delete  Int    @db.TinyInt

  @@index([formula_id], map: "formula_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model train_solution_t {
  user_id     Int
  formula_id  Int
  solution    String @db.VarChar(255)
  train_count Int

  @@id([user_id, formula_id, solution])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model train_t {
  user_id       Int
  train_type    Int
  formula_id    Int
  formula       String  @db.VarChar(1023)
  formula_name  String  @default("") @db.VarChar(1023)
  orientation   String  @db.VarChar(1023)
  state_id      Int
  init_state_id String  @db.VarChar(1023)
  train_count   Int     @default(0)
  again_count   Int     @default(0)
  total_mtime   Int     @default(0)
  total_turn    Int     @default(0)
  grasp         Boolean @default(false)

  @@id([user_id, train_type, formula_id])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model user_acc_t {
  user_id      Int    @id
  acc_name     String @default("") @db.VarChar(255)
  channel_id   Int    @default(0)
  password     String @db.VarChar(255)
  machine_code String @db.VarChar(255)
  phone_num    String @db.VarChar(255)
  phone_time   Int    @db.UnsignedInt
  email        String @db.VarChar(255)
  email_time   Int    @db.UnsignedInt
  oauth_type   Int    @db.TinyInt
  oauth_acc    String @db.VarChar(255)
  oauth_json   Json
  reg_time     Int    @default(0) @db.UnsignedInt
  reg_zone_sec Int    @default(0)
  reg_ip       String @db.VarChar(255)
  reg_country  String @db.VarChar(255)
  reg_province String @db.VarChar(255)
  reg_city     String @db.VarChar(255)
  token        String @default("") @db.VarChar(255)

  @@index([acc_name], map: "acc_name")
  @@index([email], map: "email")
  @@index([machine_code], map: "machine_code")
  @@index([oauth_type, oauth_acc], map: "oauth")
  @@index([phone_num], map: "phone_num")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model user_activity_1_t {
  user_id            Int @id
  login_num_1        Int @db.TinyInt
  comp_multi_num     Int
  comp_common_num    Int
  login_num_2        Int @db.TinyInt
  smart_train_num    Int
  common_train_num   Int
  login_num_3        Int @db.TinyInt
  standard_clock_num Int
  common_clock_num   Int
  login_num_4        Int @db.TinyInt
  comp_1v1_num       Int
  login_num_5        Int @db.TinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model user_activity_t {
  user_id             Int    @id
  city_rank_province  String @db.VarChar(255)
  city_rank_city      String @db.VarChar(255)
  city_rank_clock_num Int

  @@index([city_rank_clock_num], map: "city_rank_clock_num")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model user_ai_teach_t {
  user_id     Int
  lesson_id   Int
  chapter_id  Int
  start_time  Int    @db.UnsignedInt
  finish_time Int    @db.UnsignedInt
  duration    Int
  is_photo    Int    @db.TinyInt
  cube_state  String @db.VarChar(10230)
  is_pass     Int    @db.TinyInt

  @@index([chapter_id], map: "chapter_id")
  @@index([lesson_id], map: "lesson_id")
  @@index([user_id], map: "user_id")
  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model user_assets_t {
  user_id          Int    @id
  address_l        String @db.MediumText
  score            Int
  ticket           Int
  item_l           String @db.MediumText
  lotto_turn_log_l String @db.MediumText
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model user_comp_1v1_t {
  user_id         Int @id
  enter_num       Int
  finish_num      Int
  win_num         Int
  draw_num        Int
  win_streak      Int @db.SmallInt
  best_win_streak Int @db.SmallInt
  best_ao5        Int
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model user_comp_multi_t {
  user_id    Int @id
  enter_num  Int
  finish_num Int
  best_ao5   Int
  best_rank  Int @db.TinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model user_comp_multi_team_t {
  user_id    Int @id
  enter_num  Int
  finish_num Int
  best_ao5   Int
  best_rank  Int @db.TinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model user_cube_clone_t {
  user_id         Int    @id
  num             Int    @db.TinyInt
  add_num_time    Int    @db.UnsignedInt
  this_start_time Int    @db.UnsignedInt
  used_num        Int
  finish_num      Int
  best_duration   Int
  best_ao5        Int
  best_ao12       Int
  best_ao100      Int
  best_duration_l String @db.VarChar(1024)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model user_cube_t {
  user_id    Int    @id
  type       Int    @db.TinyInt
  suit_id    Int    @db.TinyInt
  suit_l     String @db.VarChar(14336)
  recently_l String @db.VarChar(1023)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model user_custom_avatar_t {
  id            Int     @id @default(autoincrement())
  user_id       Int
  type          Int     @db.TinyInt
  custom_avatar String  @db.VarChar(2000)
  id_photo      String? @db.VarChar(2000)
  status        Int     @db.TinyInt
  upload_time   Int     @db.UnsignedInt

  @@index([user_id], map: "user_id")
}

model user_devices_binding {
  user_id     Int      @id
  devices     Json?
  inserted_at DateTime @db.DateTime(0)
  updated_at  DateTime @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model user_goal_t {
  user_id        Int    @id
  goal_l         String @db.MediumText
  task_control_m String @db.MediumText
  task_l         String @db.MediumText
  achieve_l      String @db.MediumText
  app_eval_l     String @db.MediumText
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model user_inform_t {
  user_id     Int    @id
  auto_id     Int
  read_list   String @db.MediumText
  inform_list String @db.MediumText
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model user_misc_t {
  user_id            Int    @id @db.UnsignedInt
  misc_m             String @db.MediumText
  client_data        String @db.MediumText
  clock_group_m      String @db.MediumText
  train_repository_l String @db.MediumText
  robot_repository_l String @db.MediumText
  ai_teach_l         String @db.MediumText
  comp_misc_m        String @db.MediumText
  rank_m             String @db.MediumText
  avatar_box_l       String @db.MediumText
  enter_effects_l    String @db.MediumText
  recover_effects_l  String @db.MediumText
  achieve_m          String @db.MediumText
  app_eval_m         String @db.MediumText
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model user_qualifier_t {
  id             Int    @id
  user_id        Int
  period         Int
  cup            Int
  max_cup        Int
  win            Int
  win_streak     Int
  max_win_streak Int
  recently_win   String @db.VarChar(10240)
  lose           Int
  drawn          Int
  is_reward      Int    @db.TinyInt

  @@index([period], map: "period")
  @@index([user_id], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model user_relation_t {
  user_id       Int
  oppo_id       Int
  type          Int      @db.TinyInt
  apply_msg     String   @default("") @db.VarChar(255)
  relation_time Int      @db.UnsignedInt
  gmt_modified  DateTime @default(now()) @db.Timestamp(0)
  gmt_create    DateTime @default(now()) @db.Timestamp(0)

  @@id([user_id, oppo_id])
  @@index([oppo_id, type], map: "oppo_id")
  @@index([user_id, type], map: "user_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model user_t {
  user_id            Int    @id @db.UnsignedInt
  nickname           String @db.VarChar(255)
  avatar             String @default("") @db.VarChar(2000)
  avatar_box_id      Int    @default(0) @db.UnsignedInt
  enter_effects_id   Int    @default(0) @db.UnsignedInt
  recover_effects_id Int    @default(0) @db.UnsignedInt
  flag_id            Int    @default(0)
  background         String @default("") @db.VarChar(255)
  signature          String @default("") @db.VarChar(100)
  sex                Int    @default(0) @db.UnsignedTinyInt
  level              Int    @default(0)
  card_lv            Int    @default(0) @db.TinyInt
  city               String @db.VarChar(255)
  status             Int    @db.TinyInt
  user_type          Int    @db.TinyInt
  acc_type           Int    @default(0) @db.TinyInt
  mute_time          Int    @db.UnsignedInt

  @@index([acc_type], map: "acc_type")
  @@index([city], map: "city")
  @@index([level], map: "level")
  @@index([nickname], map: "nickname")
  @@index([status], map: "status")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model user_time_t {
  user_id             Int      @id
  register_time       Int      @default(0) @db.UnsignedInt
  local_register_date DateTime @db.Date
  last_login_time     Int      @default(0) @db.UnsignedInt
  last_logout_time    Int      @default(0) @db.UnsignedInt
  d_reset_time        Int      @default(0) @db.UnsignedInt
  zone_d_reset_time   Int      @default(0) @db.UnsignedInt
  keep_mark           Int      @default(0) @db.UnsignedInt

  @@index([keep_mark], map: "keep_mark")
  @@index([local_register_date], map: "local_register_date")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model user_time_trial_t {
  user_id         Int    @id
  num             Int    @db.TinyInt
  add_num_time    Int    @db.UnsignedInt
  this_start_time Int    @db.UnsignedInt
  used_num        Int
  finish_num      Int
  best_duration   Int
  best_ao5        Int
  best_ao12       Int
  best_ao100      Int
  best_duration_l String @db.VarChar(1024)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model user_token_t {
  user_id     Int @id
  token       Int @db.UnsignedInt
  expire_time Int @db.UnsignedInt
}

model wca_competitions {
  id          String   @id @db.VarChar(255)
  value       Json?
  inserted_at DateTime @db.DateTime(0)
  updated_at  DateTime @db.DateTime(0)
}

model wca_rankings {
  id          String   @id @db.VarChar(255)
  value       Json?
  inserted_at DateTime @db.DateTime(0)
  updated_at  DateTime @db.DateTime(0)
}
